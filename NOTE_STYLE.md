# 笔记写作规范

这个文件统一记录课程笔记整理规则。新建或更新课程笔记时，优先参考本文件。

## 内容整理规范

### 课程笔记结构

课程笔记标题最多使用三级编号：

- `1.`
- `1.1`
- `1.1.1`

不要继续拆到 `1.1.1.1`。如果需要补充细节，优先使用段落、列表或代码块。

### 课程视频笔记

视频课程整理为 Markdown 时，优先采用结构化归纳，不逐字照搬转写稿。

- 保留课程主线、关键概念、操作步骤和注意事项。
- 明显语音识别错误应结合上下文校正。
- 无法确认的术语不要强行改写，必要时保留原词或在后续校对。
- 主 Markdown 放最终笔记；字幕、转写稿、临时笔记放课程目录下的 `_transcribe`。
- 图片、截图、封面等站点资源放课程目录下的 `assets`。

### 图片插入

课程截图用于补充说明时，默认放在对应说明、步骤或无序列表之后，让读者先知道“看什么”，再看图确认。

- 如果图片是概念引入图或封面图，可以放在段落前。
- 如果图片用于解释某组功能、特点或操作项，优先放在这组列表之后。
- 图片块前后保留空行。
- VitePress 页面中的本地图片优先使用 HTML 图片块，路径相对于当前 Markdown 文件，默认不固定宽度：

```md
<p align="center"><img src="./assets/example.png" alt="图片说明"></p>
```

- 只有需要控制显示尺寸、居中缩略图或并排展示时，才按具体场景添加 `width`。

## Markdown 格式规范

### 列表标点

无序列表根据条目性质决定是否添加中文句号：

- 短语型条目不加句号，例如工具名、关键词、目录项、步骤名。
- 标签式条目通常不加句号，例如“想清楚：知道要讲什么”。
- 完整陈述句加中文句号，例如总结性观点、解释性判断。
- 同一组列表内部尽量保持一致。如果一组条目混合了短语和完整句，优先改写成同一种形式。

示例：

```md
- 幕布
- XMind
- 百度脑图
```

```md
- 想清楚：知道要讲什么
- 说明白：知道按什么顺序讲
- 呈现出来：知道用什么形式放到页面上
```

```md
- PPT 不是论文搬运，而是面向答辩场景的重新表达。
- 思维导图是 PPT 制作前的结构草稿。
```

### 空行

- 标题前后保留空行。
- 列表前后保留空行。
- 代码块前后保留空行。
- 引用块前后保留空行。
- 分隔线前后保留空行。
- 表格前后保留空行。
- YAML front matter 和正文标题之间保留空行。
- 每个段落前后保证且只有一个空行。
- 文档结尾保留一行空行。
- 连续空行通常压缩为一个空行。
- 列表项之间不留空行。

### 标点和空格

- 中文正文优先使用中文标点。
- 英文术语、快捷键、文件名、命令和代码标识用反引号包裹。
- 中日韩文字与英文或数字之间保留一个空格。
- 移除每行末尾多余空格。
- 移除链接文本首尾空格。
- 列表标志和 checkbox 后保留一个空格。

### 标题

- 不自动把标题改为大写。
- 不用文件名自动插入 H1 标题。
- 不自动递增标题级别。
- 保留标题本身的层级和语义。
- 移除标题末尾多余标点，主要包括 `;`、`?`、`!`、`。`、`，`、`、`、`；`、`：`、`！`。

### 引用块

- 引用块标志统一为有空格样式，即 `> 引用内容`。
- 保持引用块样式一致。

### 列表

- 将非标准无序列表标志统一为 Markdown 无序列表标志。
- 不自动修正有序列表编号。
- 不自动移除重复列表标志和空列表标志，避免误改课程示例。

### 代码块

- 不自动为代码块添加默认语言。
- 课程笔记中如果明确知道语言，应手动写明代码块语言，例如 `powershell`、`python`、`text`、`md`。

### YAML front matter

课程笔记默认不强制添加 YAML front matter。若某篇文章需要 YAML，则遵循以下规则：

- YAML 结束后保留空行。
- YAML 中的 `tags` 和 `aliases` 使用单行数组样式。
- YAML 多行数组不主动改写为单行数组。
- YAML 键按指定顺序排序时，允许移除多余空行。
- YAML 键排序优先项：`创建时间`、`修改时间`。
- 其他 YAML 键按字母顺序升序排列。
- YAML 排序键放在 front matter 开头。
- 时间戳格式使用 `YYYY-MM-DD HH:mm`。
- 本地时间不转为 UTC。

### 待补充

如果 Obsidian Linter 后续新增规则，继续补充到本文件，不另建规范文件。
